import { View, Text, Modal, TextInput, Button } from "react-native";
import styles from "./styles";
import { useState } from "react";

export default function App() {
  const { containert } = styles;

  return (
    <View style={container}>
      <Text>Hello</Text>
    </View>
  );
}
