import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginTop: 40,
    backgroundColor: "#fff",
    flex: 1,
    flexDirection: "row",
  },
});

export default styles;
